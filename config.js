/* ============================================================================
 * FG SALGADOS — Configuração do PIX dinâmico
 *
 * Preencha FG_CONFIG.pixApiUrl com a URL pública do backend de pagamento
 * (ex.: https://fg-salgados-api.onrender.com/api) e carregue este arquivo
 * ANTES do script.js no index.html.
 *
 * Se ficar vazio, o site usa o QR Code PIX estático atual (nada quebra).
 * ========================================================================== */
window.FG_CONFIG = {
    pixApiUrl: '' // ex.: 'https://fg-salgados-api.onrender.com/api'
};
